# <领域名> 工作区

> 全局规则照常生效,此处只写本领域附加规则。

## 每次对话开场

先读 `state/` 下的文件,拿到当前进展再回答。不要反问用户「你现在什么情况」——答案在文件里。

## 本领域回答规范

<写死你在这个领域最容易被糊弄的地方。例如:>

| 项 | 要求 |
|---|---|
| 出处 | 任何论断给来源,查不到就说「这是推断,无依据」 |
| 边界 | 结论适用范围说清楚,别把特例当通则 |
| 拒绝 | 不给安慰,只给判断 |

## 目录分工

| 目录 | 放什么 | 进 git |
|---|---|---|
| `knowledge/` | 精炼后的结论、原则 | ✅ |
| `state/` | 具体进展、对具体对象的判断 | 看仓库可见性 |
| `material/` | 原始素材,加工完可删 | ❌ |

## 大批量原始材料

用户扔进来超过 50 行的原始记录时,派 subagent 去啃,只把结论带回主窗口,不在主窗口复述原文。

## 仓库可见性

本仓库是 <Public / Private>。<如果 Private,写明什么绝不能外流;如果 Public,写明什么绝不能进来。>
